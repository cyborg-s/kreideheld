# docker-compose.yml im Projekt-Root
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: kreideheld
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: kreideheld
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: